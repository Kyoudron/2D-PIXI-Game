A Simple 2D game using PIXI JS.

Fork/Clone the file

`npm install` all required node files

`npm start` to start the game

Get the chest without being hit. Return back to the key door to win. Simple.